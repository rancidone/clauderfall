# Clauderfall v1 Archive

This directory holds the pre-v2 implementation that previously lived under `src/clauderfall/`.

- `legacy/v1/src/clauderfall_v1/` contains the archived v1 package.
- `legacy/v1/tests/` contains the archived v1 test suite.

The active implementation space is now `src/clauderfall/`.
